#!/bin/bash
set -e

if ! [ -L ~/.gnupg ] || ! [ -L ~/.ssh ]
then
  echo "Run script/setup first!" >&2
  exit 1
fi

onepassword_login() {
  if ! command -v op >/dev/null
  then
    echo "Install op first!" >&2
    exit 1
  fi

  # shellcheck disable=SC2154
  if [ -z "$OP_SESSION_wdeboer" ]
  then
    echo "Logging into 1Password..."
    eval "$(op signin wdeboer.1password.com weyert@innerfuse.biz)"
  fi
}

onepassword_get() {
  if [ -f "$HOME/$2" ]
  then
    echo "$2 already exists."
    return
  fi
  echo "Extracting $2..."
  onepassword_login
  op list items
  op get document "$1" > "$HOME/$2"
  chmod 600 "$HOME/$2"
}

onepassword_get zagw7lmcsbhszhkfpxeacdyhza .ssh/id_rsa
onepassword_get zagw7lmcsbhszhkfpxeacdyhza .ssh/id_rsa_kikkernet
onepassword_get zagw7lmcsbhszhkfpxeacdyhza .ssh/id_keytree_rsa
onepassword_get 3sjbzdhzfzcr3o52qrojwjgjjq .gnupg/weyert@innerfuse.biz.private.gpg-key

if ! [ -f "$HOME/.secrets" ]
then
  echo "Extracting secrets..."
  if ! command -v jq >/dev/null
  then
    echo "Install jq first!" >&2
    exit 1
  fi
  onepassword_login
  GITHUB_TOKEN=$(op get item g6klazxi3ffprbnou2mexznlay | jq -r '.details.sections[1].fields[].v')
  cat > "$HOME/.secrets" <<EOF
export GITHUB_TOKEN=$GITHUB_TOKEN
EOF
  chmod 600 "$HOME/.secrets"
fi

echo "Storing SSH keys in keychain..."
ssh-add -K

echo "Setting up GPG..."
if ! command -v gpg >/dev/null
then
    echo "Install GPG first!" >&2
    exit 1
fi

chmod 700 ~/.gnupg
gpg --import ~/.gnupg/weyert@innerfuse.biz.public.gpg-key \
             ~/.gnupg/weyert@innerfuse.biz.private.gpg-key
